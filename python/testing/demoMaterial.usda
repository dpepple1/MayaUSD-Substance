#usda 1.0

def "mtl"
{
    def Material "demoMat"
    {
        token outputs:surface.connect = </mtl/demoMat/demoSurface.outputs:surface>

        def Shader "demoSurface"
        {
            uniform token info:id = "UsdPreviewSurface"
            token outputs:surface
        }
    }
}

